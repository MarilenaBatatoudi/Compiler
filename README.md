# Compiler
MiniLang compiler in C++: Stage 1: Lexical Analyzer, Stage 2: Parser (Parser Generator, Header for AST),  Stage 3: semantic analyzer with symbol tables &amp; control-flow checks, Stage 4: MIPS code generator producing SPIM assembly.
